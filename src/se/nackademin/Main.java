package se.nackademin;import se.nackademin.model.*;import javax.swing.*;public class Main {    public static void main(String[] args) {        Feedable feedable;        while (true) {            String name = JOptionPane.showInputDialog("Vilken växt ska få vätska?");        //Medelandet som dyker up i rutan            feedable = GreenestManager.getPlantInstance(name);              //getPlantInstance metoden från GreenSetManager tar en sträng och lägger i feedable variable.            if (feedable == null) {                //Om den strängen är fell så feedable är null                int answer = JOptionPane.showConfirmDialog(null, "Please write correct one, The correct one should be one of the below:\nIGGE, LAURA, MEATLOAF, PUTTE\nDo you want to continue?");                if (answer == 1 || answer == 2) {      //Om användaren vill inte fortsätta vidare och anger "nej" (= nummer 1) i rutan elle "cancel" (nummer 2)                    break;       // break gör så att går ut av loopen                } else {                    continue;   //Annars fortsätter till början att fråga igen.                }            }            JOptionPane.showMessageDialog(null, feedable.feed());       //Numeriska värdena som räknades, med de strängar (enligt angiven växttyp) kommer att visas i rutan.            int answer = JOptionPane.showConfirmDialog(null, "Do you want to continue?");       //Medelandet som dyker up i rutan.            if (answer == 1 || answer == 2) {          //Liksom förra gången här uppe                break;            }        }    }}